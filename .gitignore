node_modules/
dist/
.DS_Store
*.local

# Matière source : jamais versionnée, jamais publiée. La barre initiale ancre
# la règle à la racine, sinon elle attrape aussi `public/media/`.
/media/
# Les proxies générés par `npm run media:build` sont versionnés : la CI n'a pas
# accès à `media/`, et GitHub Pages ne sert que ce qui se trouve dans le repo.
