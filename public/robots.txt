# L'exploration est autorisée, et c'est délibéré : un `Disallow` empêcherait
# aussi les robots d'aperçu (Slack, Discord, Facebook, LinkedIn, WhatsApp) de
# lire la page, et les liens partagés n'auraient aucune vignette.
#
# L'indexation est bloquée par la balise `noindex` de index.html. C'est le
# mécanisme correct : un robot doit pouvoir lire la page pour y voir le
# `noindex`, ce qu'un `Disallow` lui interdisait justement.
User-agent: *
Allow: /
